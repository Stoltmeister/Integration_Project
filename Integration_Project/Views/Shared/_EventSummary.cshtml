@model Integration_Project.Models.Event

<div>

    <div class="thumbnail">
        @{
            var imgSrc = "Hello";
            if (Model.EventPicture != null)
            {
                var base64 = Convert.ToBase64String(Model.EventPicture);
                imgSrc = String.Format("data:image/gif;base64,{0}", base64);
            }
            else
            {
                imgSrc = "https://images.pexels.com/photos/534031/pexels-photo-534031.jpeg";
            }

        }
        <img src=@imgSrc alt=<img src="https://images.pexels.com/photos/534031/pexels-photo-534031.jpeg" />
        <div class="row">
            <div class="caption">
                <div class="col-sm-12">
                    <h4><a asp-controller="Events" asp-action="Details" asp-route-id="@Model.Id">
                        @{if (Model.Name == null)
                            {
                                @:ThisEventHasNoName
                                  }
                            else
                            {
                                @:@Model.Name
                            }
                        }
                    </a></h4>
                </div>
            </div>
        </div>
    </div>
</div>